---
"@yadsh/dsh-session-scope": patch
---

Internal cleanup: the legacy `release:prepare` script from the previous release scheme is removed, and package verification gates run through the shared runner. No runtime changes.
