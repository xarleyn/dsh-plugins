---
"@yadsh/dsh-qa-integrations": patch
---

The provider specifications, the TeamCity branch hint and its fixtures no longer
name an internal Jira project: every example and every test datum uses the
neutral `PROJ` placeholder, so the shipped examples describe no real project.
