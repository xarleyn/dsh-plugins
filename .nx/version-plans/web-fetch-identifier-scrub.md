---
"@yadsh/dsh-web-fetch-authenticated": patch
---

The shared-hostname example in the README uses the repository's
`jira.example.corp` placeholder instead of a real corporate host, so the rule it
illustrates reads the same way without naming a deployment.
