{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/xarleyn/dsh-plugins/blob/main/docs/plugins.schema.json",
  "title": "dsh-plugins catalog manifest",
  "description": "Machine-readable catalog of the DeepSeek Harness (DSH) packages published from the xarleyn/dsh-plugins monorepo. Generated by scripts/generate-plugins-manifest.mjs and verified by scripts/verify-package-hygiene.mjs.",
  "type": "object",
  "additionalProperties": false,
  "required": ["generatedBy", "repository", "githubTopic", "plugins"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Relative path of this schema."
    },
    "generatedBy": {
      "type": "string",
      "description": "Repository script that regenerates this file."
    },
    "repository": {
      "type": "string",
      "format": "uri",
      "description": "Canonical git repository hosting every listed package."
    },
    "githubTopic": {
      "type": "string",
      "description": "GitHub topic every package repository carries, used by DSH plugin indexers that discover packages by topic."
    },
    "plugins": {
      "type": "array",
      "description": "Every publishable package in the monorepo, ordered by directory.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "name",
          "npm",
          "path",
          "description",
          "keywords",
          "install",
          "homepage",
          "client"
        ],
        "properties": {
          "name": {
            "type": "string",
            "description": "Workspace directory name, which is also the runtime DSH plugin id."
          },
          "npm": {
            "type": "string",
            "description": "Full npm package name, including its scope."
          },
          "path": {
            "type": "string",
            "description": "Monorepo directory of the package, relative to the repository root."
          },
          "description": {
            "type": "string",
            "description": "Package description, copied from its package.json."
          },
          "keywords": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Package keywords, copied from its package.json."
          },
          "install": {
            "type": "string",
            "description": "Install command for a DSH profile."
          },
          "homepage": {
            "type": "string",
            "format": "uri",
            "description": "Documentation entry point for the package."
          },
          "client": {
            "type": "boolean",
            "description": "True when the package ships a browser client bundle."
          }
        }
      }
    }
  }
}
